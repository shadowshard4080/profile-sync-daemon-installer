# profile-sync-daemon-installer
Script to install profile-sync-daemon on Arch Linux. Supports Brave browser and more.

Download psd-installer.sh

```
cd Downloads/
chmod +x psd-installer.sh
bash psd-installer.sh
```
